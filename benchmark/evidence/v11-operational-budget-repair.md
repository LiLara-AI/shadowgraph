# Operational provider-budget repair — offline candidate

> ## Current prospective status — Amendment 006 (committed exact candidate; not a live result)
>
> This header is the current operational disposition. It supersedes only the current-status reading of later Amendment 005 passages below; it does not alter frozen predecessor bytes, historical run-002/run-003 evidence, or the closed results-cleanup decision.
>
> **Proven current source/binding:** Amendment 006 (`3bc9308a19e44ecc06d15dc0144239aa907b49cf897a11f9fab7cfe116966760`) binds an arm-neutral meter-owned root trace. The harness rejects a repeated measured root (A); provider meter traces root operation, safe structured-output mode, model and bounded per-root/class attempts (24); C/D/B/E reconciliation is fail-closed; and raw/aggregate/bundle identity carries Amendment 006 as a seventh trusted source hash. The current clean committed candidate has an official implementation lock and an unused, exact-lock-bound `v11-acceptance-004` budget and campaign policy. Those external bindings apply only to their exact committed tree and must be rebuilt after any source change.
>
> **Proven offline:** The selected pinned Cognee 1.5.3 loopback evidence is manifest-verified: LLM malformed-output behavior has same-model C calls; embedding HTTP-500 behavior has B transport attempts before an unobserved later outer retry. The evidence gate hashes co-located report bytes, requires same package/model pins, loopback-only execution, no A/E, and complete-or-fail-closed accounting. It clears only `cognee/internal_memory_llm/C` and `cognee/embedding/B`; live meter reconciliation remains mandatory. The selected embedding factory constructs `AsyncOpenAI(..., timeout=120)` without a public `max_retries` input and wraps it in Tenacity. There is no supported benchmark configuration that disables those selected embedding transport retries without vendor modification. The LLM public `llm_args.num_retries: 0` control remains applied and is not represented as an embedding control.
>
> **Proven fresh prerequisites (not a live result):** Fresh, checksummed service evidence establishes pinned container-image identity, the configured endpoints, both locked Ollama model checks, and Neo4j `AUTH_OK` through the out-of-band `SHADOWGRAPH_NEO4J_AUTH` mapping without recording its value. A fresh Cognee ACL precondition record is `PASS`; fresh offline native B/C evidence is manifest-verified; and the structured preflight returned `READY` with zero blockers and exit 0. The existing pinned Neo4j and Ollama containers were started only after identity, digest/layer, ports, persistent mounts and non-secret configuration shape were audited; no image pull, recreation, model/provider/endpoint substitution, credential change, scored work or publication occurred.
>
> **Not measured / still unrun:** no `v11-acceptance-004` provider traffic, campaign ledger, raw run, aggregate, ranking, or publication exists. The next action remains exactly one fresh non-scored acceptance after the current exact committed source/lock/review gate; its live meter reconciliation remains mandatory. These prerequisite records do not infer any arm success or turn known arm failures into passes.

## Disposition

Hermes owns this benchmark-only repair. No product/plugin change is required by the demonstrated defect, so there is no Codex product handoff. This document supersedes the **operational budget advice** in `v11-hermes-execution-handoff-2026-09-06.md`, not any frozen methodology. Amendment 005 is a new prospective methodology source; it does not rewrite any predecessor bytes. The historical document, external scripts, run-002/run-003 evidence and state remain unchanged.

**Owner authorization is granted; live execution has not been evaluated or dispatched.** The owner permits verified local benchmark-only commits and one fresh `v11-acceptance-004`, conditional on every prerequisite. Amendment 005 now distinguishes zero harness re-execution from fully metered, bounded native provider attempts; it does not declare Cognee ready, successful, or exempt from a failed unit. No service/precondition probe or acceptance inference was dispatched in this engineering pass. Run-003 remains interrupted and is not resumed.

## Authority and corrections to the post-mortem

- `exactFiniteTotal` was created by external coordinator scripts `shadowgraph-v11-budget-resolution.py:120` and `shadowgraph-v11-budget-resolution-v2.py:27`; the latter wrote the historical budget-resolution record. `shadowgraph-v11-stop-current.py:19` subsequently used it in the stop rationale. It was not a repository launch predicate or a frozen methodology field.
- Amendment 002, `resolvedMethodologyChoices.measuredOperationRetries` (lines 33–38), froze zero measured retries and disabled library defaults without a taxonomy for a product-native provider attempt inside one root operation. Amendment 005 prospectively refines that ambiguity: A (harness re-execution) and outer-decision retries remain zero; publicly configurable B transport controls must be zero; C/D/native library attempts are allowed only when wire-metered, same-model/endpoint, finite under the existing class caps/timeouts/deadline, and never relabel a failed root operation as a harness rerun; E model/provider fallback remains prohibited.
- Amendment 005 applies identically to every arm. It changes no arm, model, endpoint, phase, scenario, seed, repetition, timeout, class cap, score, public claim threshold, or historical run artifact. An arm relying on C or D must provide selected-runtime loopback fault evidence before live dispatch; a ledger discrepancy or exhausted native behavior remains a FAILED unit.
- The launch error was failure to resolve and enforce an explicit finite operational authorization before dispatch. Treating `exactFiniteTotal:false` itself as a frozen-contract violation was an incorrect explanation. The intentional external SIGTERM remains the recorded interruption cause; no plugin crash is established by that fact.
- The old statement “zero integrity violations” was too strong. The historical completion ledger has no write-ahead attempts or provider-event wall-clock timestamps. Its successful rows do not prove that no request was in flight at termination, no attempt was omitted, or that all retry/state/prompt/model constraints were fully reconciled. No completed run-003 raw/aggregate/reconciliation artifacts exist.

## Four quantities that must not be conflated

| Quantity | Meaning / authority |
|---|---|
| 260 | Deterministic outer-decision **slots**, also declared in Amendment 003. Derived as non-excluded units minus RESET units. An early arm failure may prevent a scheduled outer call; this is not a promise of 260 successes. |
| Native internal/embedding calls | Data-dependent. `ProviderCalls` increments observed calls (`python_runtime.py:145–175`); `require_traffic` means at least one on the branch that invokes it, not exactly one. Cognee's absent-dataset retrieval explicitly makes zero calls (`cognee_adapter.py:499–514`). |
| 264 / 308 / ~320 | Historical handoff lines 280–284 reuse run-002 expected/matched 264 and observed 308 as future-budget guidance, with an approximate ~320 stop heuristic. Neither is a valid authorization for the changed candidate or a forecast for run-003. |
| 588 | Retracted coordinator forecast based on fixed native multiplicities; not a frozen or approved safety ceiling. Do not reinstate it. |
| Finite safety ceilings | Explicit operator-chosen admission limits for **each** of the three request classes. They are not expected call counts, retry allowances, success promises, or reconciliation tolerances. |
| Observed completion rows | Run-003 preserved rows: 64, comprising 43 outer and 21 embedding, all SUCCEEDED. These are recorded terminal events, **not a proven total of upstream attempts**. Partial terminal unit rows: 62, comprising 48 MEASURED, 10 FAILED and 4 EXCLUDED. |

The external script's previous native “minimum” values (44/160) are not adopted as limits or authoritative forecasts. Its Mem0 calculation assumes nine decision phases while Mem0 supports the user-isolation phase; its Cognee calculation treats absent-dataset retrievals as calls. No cap may be silently derived from those assumptions.

## Implemented launch and accounting policy

1. `v11-preflight` and `v11-run` read an explicitly supplied `--provider-budget` JSON file. Missing, malformed, non-finite, retry-enabled-at-the-harness layer or identity-mismatched policy blocks readiness. There is no guessed numeric default and no `exactFiniteTotal` guard to delete.
2. Budget files must explicitly name `--run-id` and `--attempt-id`, an operator authorization reference, the **new** canonical implementation-lock hash, `maxRetries:0` for harness/outer-operation re-execution, and finite non-negative safe-integer limits for `outer_decision_llm`, `internal_memory_llm`, and `embedding`. `maxRetries:0` does not erase an underlying library provider request; every such admitted request remains a metered attempt under Amendment 005.
3. Readiness refuses before runtime binding. Binding independently validates budget identity before runtime discovery; after building the canonical implementation lock it checks the approved hash **before environment observation, directories, meter, hosts or ledgers**. The budget snapshot is cloned/frozen and passed through to execution and the meter.
4. The meter synchronously reserves a class slot before any asynchronous work. Admitted malformed, timed-out and failed requests consume their slots; no refunds. Concurrent calls cannot oversubscribe. The first denied request latches an attempt-wide stop on new admissions. Already admitted in-flight requests remain within the reserved ceilings. A denial is HTTP 403 and a FAILED `PROVIDER_BUDGET_EXHAUSTED` completion, never a silent exclusion or upstream retry.
5. The existing provider completion schema remains unchanged. A new exclusive companion `<provider-ledger>.attempts.ndjson` captures the authorization, timestamped admissions, write-ahead dispatch intents and completion links. Its writes are synchronized before upstream dispatch; an evidence-write failure prevents later dispatch and makes close fail.
6. A dispatch intent is **not proof of provider receipt**: a process can die between journal sync and socket send. An unmatched intent/admission stays incomplete/unknown. Completed, admitted, denied, succeeded, failed and incomplete counts are separate.
7. New-run reconciliation requires the companion ledger as well as existing model/usage/count reconciliation. Missing, malformed, mismatched, failed or incomplete attempt evidence blocks the combined verdict. Legacy historical reconciliation remains readable without inventing historical attempt rows.
8. The generic stand-alone meter API retains its legacy unbudgeted mode for existing non-acceptance callers. The v1.1 acceptance binding never uses that mode. This is an operational acceptance boundary, not an OS-wide egress firewall or protection against a malicious operator changing source code.
9. Amendment 005 is the effective prospective retry taxonomy. A is prohibited and represented by `harnessOperationRetries:0`; public B controls are set to zero where the selected library exposes them; C/D or unconfigurable native B attempts are not secretly credited or ignored. They are admitted and metered before dispatch, reconciled against the adapter operation counters, charged to the same class caps/latency/tokens/cost evidence, and fail closed on ledger disagreement, cap exhaustion, timeout, model/provider substitution, or `UNVERIFIED_OPERATION_COUNT` from a failed root operation. Existing `RETRY_OBSERVED` and `UNEXPECTED_CALL` checks still reject unaccounted traffic.

## Final owner-authorized single-run limits

- Owner-authorized aggregate outer ceiling: **260 admitted calls** across all arms, not per arm.
- Owner-authorized single-run native internal-memory ceiling: **256 admitted calls**.
- Owner-authorized single-run embedding ceiling: **1,024 admitted calls**.
- Owner-authorized single-run `maxRetries`: **0 harness/outer-operation re-executions**. It is not a promise that a selected native library will make only one provider request inside a root operation.
- The three acceptance caps sum to **1,540**, but this is NOT a shared pool and NOT a probe-inclusive budget. Probe traffic must be recorded separately. No operational campaign was initialized and no deadline started while this candidate awaits its final commit/lock/review and fresh live prerequisites. The campaign library supports an immutable absolute deadline, cumulative request/class ceilings and recovery limit; that capability is not a claim that all probe routes are integrated. The final owner authority permits **one acceptance attempt, zero recovery attempts**, one service probe and one precondition probe.
- These native values are discretionary operational stop-losses, not exact forecasts or reconciliation targets. Amendment 005 makes every admitted native request finite at the run level through the independent class ceilings and deadline/timeouts; it does not invent a per-operation forecast or guarantee that call-heavy product behavior completes.
- The owner must not treat a ceiling crossing as a product failure: it is a contained diagnostic interruption with preserved attempts and incomplete reconciliation. The campaign policy itself is versioned, identity-bound and immutable after creation.
- The owner explicitly authorized local commits of verified benchmark-only changes. No push, merge, release, tag, or product change is authorized.

A budget is a stop-loss, not permission to alter a failed run after the fact. Never raise a ceiling in a running attempt, reuse a consumed identity, retry measured operations, or relabel a budget-limited run as completed acceptance. A ceiling can halt a healthy but call-heavy native branch; that is an operational interruption, not evidence that the product failed its intended task.

## New candidate identity and remaining gates

The Amendment 005 candidate is an **uncommitted** benchmark-only delta based on `40f2454585356ee8482a344c17243a4e4500631a`. The prior official implementation lock does not cover these bytes. No package version, product source, competitor source, provider/model pin, or historical result is changed.

The external evidence directory `~/shadowgraph-v11-run-evidence/budget-repair-offline/` contains the earlier working-copy file-hash manifest, binary diff, preservation audit, test logs and canonical builder refusal. The working-copy manifest is **not** an official implementation lock. The canonical builder's clean-committed-tree requirement is retained. The final external report must record the new commit and builder result, not infer them from this pre-commit document.

Required order: complete current offline conformance and independent code/methodology review; commit named verified files; create/verify a new implementation lock; bind exact campaign policy/configuration/coordinator bytes; refresh permitted service and precondition evidence; require structured `READY` plus exit 0; then use only a new `v11-acceptance-004` run id and namespaces. Historical service evidence can supply offline candidate-lock identity inputs, but cannot clear live freshness. Amendment 005 clears the former taxonomy ambiguity; it does not bypass any other gate.

Known Graphiti LB2f and Basic Memory F28 outcomes remain failures, not passes or exclusions. Cognee ACL prerequisites must be demonstrated before dispatch; a known possible failure cannot bypass readiness. Passing operational gates does not guarantee successful seven-arm acceptance.

## Verification and deferred disposition

- Full-worktree gate: **CLOSED without weakening the no-results guard.** `benchmark-v11-definition.test.js` changed only to bind the new amendment source chain; its existing no-results assertion remains intact. `benchmark/results/` is absent locally, so the guard runs in its intended clean-candidate state.
- Current preservation is **PROVEN** by `~/shadowgraph-v11-run-evidence/005-methodology/preservation-hardening-20260908T063214Z.json` (SHA-256 `4cfe077caa827b11e12767566aea2605974b0708163279305579d9434b9b0a23`): all run-002/run-003/archive manifests verified, the archive's three payload hashes equal preserved run-003 hashes, and ordinary directory write bits were removed (`0555`, `0555`, `0500`). This is filesystem hardening, **not** WORM/object-lock evidence.
- Historical archive-before-removal ordering is **NOT_PROVABLE_FROM_CURRENT_FILES**: present manifests prove current bytes, not the timestamped order of the prior copy/verify/delete operation. The earlier action is retained as session evidence only and is not represented as an independently durable ordering proof. This limitation has no effect on the clean-candidate gate, but must remain in any handoff.
- Deferred ordering-evidence record: **reason** a durable event log was not created at the historical cleanup; **impact** no absolute archive-before-removal claim; **workaround** current manifest audit plus filesystem hardening; **non-collapse test** synthetic external procedure proof `archive-procedure-noncollapse-20260908T063321Z.json` (SHA-256 `f2a7ead2219848178d3fc23b49902ec7335917dca2668235e49994f72f4b9a56`); **next action** preserve the generated audit and use an ordered durable record for any future cleanup; **decision/ADR state** NOT_MEASURED, no historical rewrite.
- Final recovery offline gates: Node **2,456 total; 2,341 pass; 0 fail; 115 skipped**; Python adapters **99/99** with bytecode cache absent; `benchmark:check` exit `0`; package check exit `0` (`129` files, `private=true`). Decision state: **CLOSED for the no-results guard**, with historical archive-order evidence still explicitly NOT_PROVABLE.
- Review records are snapshot-specific. The external closure record must distinguish completed reviews, unavailable attempts, unchanged reviewed code, and documentation-only deltas. This pre-commit document is not itself proof that a commit, official implementation lock, or live-readiness gate completed.

- Focused offline tests exercised readiness refusal, otherwise-ready budget-only refusal, wiring to the meter, wrong implementation identity, concurrent reservation, failed-call consumption, zero meter retries, cross-class latch, completion/attempt accounting, malformed evidence and missing identity.
- An earlier bounded review identified a P2 admission-correlation omission: missing run/attempt IDs could behave as wildcards. The regression reproduced RECONCILED for a mismatched completion; production validation now requires both fields. The completed independent final changed-surface review (`deleg_b39e5035`) found no P0/P1/P2 findings and issued no live-acceptance verdict. The independent documentation-delta review recorded externally after that review must cover the final wording before staging; no live launch is claimed by either review.
- Native ceilings and local-commit authorization: CLOSED (owner-approved). Actual commit/lock/review evidence is captured externally; authorization is not evidence of completion.

## Cognee retry taxonomy and prospective resolution

The earlier finding is retained, not erased: selected Cognee 1.5.3 `litellm_native` has a three-request JSON validation/self-correction loop (`native_adapter.py:250–318`) and an outer Tenacity wrapper (`:401–497`). `retry_config.py:29–37` gives that wrapper a time/attempt floor. The historical one-call integrity assertion remains red evidence; it was not weakened or rewritten. A provider meter alone was never proof that those native paths were disabled.

Amendment 005 resolves the **classification and accounting** issue prospectively, not by asserting that Cognee has zero internal calls. The adapter now explicitly applies Cognee's public `set_llm_config` with `structured_output_framework="litellm_native"`, `llm_args={"num_retries": 0}`, and empty `fallback_model`, `fallback_api_key`, and `fallback_endpoint`. The `num_retries` control reaches the selected LiteLLM transport; the previous adapter-level `max_retries` declaration did not. Both the acceptance-definition loader and direct runner validate the canonical Amendment005 JSON **and** canonical SHA-256 sidecar before adapter/progress side effects. No vendor/product source was changed.

The governed probe `benchmark/probes/cognee_retry_taxonomy_demonstration.py` runs inside the pinned image with Docker `--network none` and the host's loopback-only socket fence. Its current offline result (captured before final commit/lock and to be rerun against the final bytes) is:

| Category | Evidence and disposition |
|---|---|
| A — harness measured-operation re-execution | **False.** The probe invokes one root native structured-output operation; no adapter `persist`/`retrieve` call is rerun. The runner separately enforces zero harness/outer-operation retries. |
| B — transport retry | **Not observed in this fault.** All three loopback responses were HTTP 200 and selected LiteLLM received `num_retries:0`. This does not claim that an unconfigurable lower native path can never retry a transport failure; any such wire attempt remains metered and capped under Amendment 005. |
| C — native validation/self-correction | **True.** A valid HTTP response containing `{}` for a required schema produced exactly three loopback requests and exactly three `ProviderCalls.internal_memory_llm` increments. |
| D — structured-output mode fallback | **False in this path.** `supports_response_schema("openai/qwen2.5:7b")` was false, so the selected client entered JSON handling directly; no transition to another structured-output mode was observed. |
| E — model/provider fallback | **False.** Fallback fields were empty; the client kept `openai/qwen2.5:7b` and the OpenAI-compatible wire body used its locked bare id `qwen2.5:7b` on all three requests. That provider-prefix translation is not a model substitution. |

After C exhausts, the probe observes the native outer wrapper scheduling a retry; it cancels the wrapper before another loopback request solely to bound the offline fault. This scheduled wrapper is **not** one of the three observed calls and is not relabelled as transport retry. In a live operation it is a native attempt subject to meter-before-dispatch, the same class cap, request/operation/unit timeout, deadline, same-model/endpoint rule, no fallback-model rule, and failed-unit treatment. It never authorizes a harness rerun.

The final recovery probe log is `~/shadowgraph-v11-run-evidence/005-methodology/cognee-retry-taxonomy-recovery-20260908T071248Z.log` (SHA-256 `a9ad73edd67286ffbef259febfcc004df1a4f8590c6570dd4fc232f86645765c`). It used Docker `--network none` plus a loopback-only socket fence and is neither provider traffic nor an acceptance unit.

This resolution is generic: the same trace/reconciliation rule applies to every arm. It does not make Cognee a pass by exception, grant a retry allowance, change the frozen model/provider, or convert a capped/failed operation into success. It removes only the invalid premise that every provider request within a product-native root operation is semantically a measured-operation retry.

## Campaign integration scope

The CLI accepts paired `--campaign-policy` and `--campaign-root`, validates supplied policy, and the runtime binding opens the persistent ledger after its implementation lock, validates lock identity, begins a unique session and passes the reservation callback to the meter. This integration is optional for legacy library callers. Stand-alone offline taxonomy probes are deliberately outside acceptance traffic and must be captured separately; their successful result is not a service probe, precondition probe, preflight, campaign activation, or live acceptance. Tests prove the covered seams only, not live readiness.

## Local fixture-ref recovery

A test-fixture Git helper inherited `GIT_DIR`/`GIT_WORK_TREE` and created accidental local commit `b5ff14f6cd463ad977e9d5d7ba9f80b49a64eef3` (`fixture baseline`, parent `40f2454585356ee8482a344c17243a4e4500631a`). **Impact:** the intended candidate bytes were captured under an unauthorized fixture identity; no remote was changed, and no reset, amend, force update, delete, push, merge, release, or tag was performed.

**Recovery:** preserve `b5ff` unmoved as forensic evidence; capture a binary base-to-candidate patch at `~/shadowgraph-v11-run-evidence/005-methodology/fixture-branch-recovery-20260908.patch` (SHA-256 `cb14b814e3f39168c0965a0d447f94b2464383aca660db8dd0648709f486cdaf`); create local branch `benchmark/v1.1-nonscored-acceptance-recovery` from `40f`; apply the checked patch and nine later test-byte deltas; verify all 25 base-relative paths byte-identical. The fixture now clears inherited Git repository selectors, with a scratch-sentinel regression proving it cannot move caller HEAD. The complete implementation-lock suite passed **15/15** on the recovered WSL worktree.

**Limitation and decision:** the recovery worktree's Git metadata was normalized only for WSL path discovery, with pre-normalization copies retained in the external evidence root. This is a local, non-published recovery artifact. ADR state: **RECOVERED_NO_HISTORY_REWRITE**; retain accidental `b5ff` and do not treat it as an authorized benchmark commit.

## Post-commit fresh readiness probes

The authorized fresh service probe at `~/shadowgraph-v11-run-evidence/005-methodology/fresh-service-probe-20260908T073513Z/` exited `1` and is manifest-verified. It observed the frozen containers' image identities but found Neo4j HTTP/Cypher and Ollama chat/embedding/model-weight checks unavailable because the expected containers were stopped. The authorized fresh Cognee ACL precondition probe at `~/shadowgraph-v11-run-evidence/005-methodology/fresh-precondition-probe-20260908T073513Z/` also exited `1`: ACL posture and distinct-user creation passed, then real ingestion timed out at the unavailable common endpoint. It is retained unchanged as a failed historical probe and was not rerun.

That precondition result exposed a benchmark-only failure-recording defect: a fatal probe record omitted package/posture fields and was classified as malformed rather than as a declared failed demonstration. The recovery candidate corrects future fatal records and `verifyPreconditionEvidence` now reaches `DEMONSTRATION_FAILED` for a fatal zero-step record; the regression was RED then GREEN. This correction does not rewrite the captured failed probe or convert it to positive evidence.

The post-probe preflight at `~/shadowgraph-v11-run-evidence/005-methodology/final-preflight-after-fresh-probes-20260908T073513Z/` exited `1` with **NOT READY** and exactly three blockers: Cognee's declared ACL precondition remains unmet, Graphiti's Neo4j+Ollama service requirement is unverified, and Cognee's common endpoint is unverified. The provider budget and lock identity passed. No `004-campaign` root, campaign ledger, acceptance raw/aggregate/reconciliation artifact, or acceptance provider dispatch was created. Decision state: **BLOCKED_EXTERNAL_INFRASTRUCTURE_UNAVAILABLE**; no service recreation, model substitution, scored work, publication, push, merge, release, or tag occurred.
